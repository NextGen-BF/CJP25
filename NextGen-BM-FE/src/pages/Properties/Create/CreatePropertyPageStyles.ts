import { SxProps } from "@mui/material";

export const createPropertyPageStyles : {[key:string]:SxProps} = {
    inputStyles: {
        flexShrink:1,
        flexBasis:'15%',
        margin: '1rem'
    }
}